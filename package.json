{
  "name": "mission-control-sample",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Disposable target repo for Mission Control acceptance runs.",
  "scripts": {
    "test": "node --test tests/"
  }
}
